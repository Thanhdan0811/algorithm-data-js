

# Suspense 
- hiển thị fallback khi ở trạng thái chờ data. 
- Suspense sẽ chỉ hoạt động với suspense-source enables data source.
    + React.lazy
    + Library như : ReactQuery, Relay, SWR.
    + `use` API.
    + Nextjs với server component.

